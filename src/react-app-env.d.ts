/// <reference types="react-scripts" />

type Props = {
  images: string[],
};

type State = {
  counter: number,
  step: number,
  itemWidth: number,
  frameSize: number,
  animationDuration: number,
};
